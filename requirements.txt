# Telegram-бот
aiogram>=3.4,<4.0

# ORM + БД (SQLite сейчас, Managed PostgreSQL позже — меняется только DATABASE_URL)
SQLAlchemy>=2.0,<3.0
psycopg[binary]>=3.1        # драйвер PostgreSQL для боевого контура

# Конфигурация
python-dotenv>=1.0
pydantic>=2.5
pydantic-settings>=2.1

# HTTP к Yandex Cloud (SpeechKit / AI Studio REST)
httpx>=0.27

# Экспорт в Excel (понадобится в контуре 3)
openpyxl>=3.1
